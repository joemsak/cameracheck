<% form_id ||= 'new_venue' %>
html = "<%= escape_javascript(render(all_venues)) %>";

$('#venues').fadeOut('fast', function(){
  $(this).html(html).fadeIn();
});

$("#<%= form_id %>").removeClass('loading');
